export interface iNumberInputSpecialProps {
    min: number,
    max: number,
    step: number,
}

export interface iTernaryNumberInputSpecificProps {
    min?: number,
    max?: number,
    step?: number,
}
